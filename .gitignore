# =========================================================================
# 华南森孚贸易有限公司 - Git Ignore 配置文件
# =========================================================================
# 文件说明：配置 Git 版本控制中需要忽略的文件和目录
# 位置：网站根目录（.gitignore）
# 用途：仅上传可公开用于 GitHub Pages 的静态网站文件
# 
# 语法规则：
#   #  - 注释行，用于说明
#   *  - 通配符，匹配任意字符
#   ?  - 通配符，匹配单个字符
#   ** - 匹配多级目录
#   !  - 否定，表示不忽略
# =========================================================================



# =========================================================================
# 敏感信息和配置文件（禁止上传）
# =========================================================================

# 环境变量和密钥文件
.env
.env.local
.env.*.local
*.pem
*.key
*.crt
*.secret
credentials.json

# -----------------------------------------------------------------------------
# 2. 开发工具与 IDE 配置（无需上传）
# -----------------------------------------------------------------------------
# VS Code
.vscode/
*.code-workspace
.history/
.github/prompts/

# IntelliJ IDEA / WebStorm
.idea/
*.iml
*.ipr
*.iws

# Lingma IDE
.lingma/

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# -----------------------------------------------------------------------------
# 3. 构建依赖与临时文件（禁止上传）
# -----------------------------------------------------------------------------
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json
yarn.lock

# 临时文件
*.tmp
*.temp
*.log
*.bak
*.swp
*.swo
*~

# -----------------------------------------------------------------------------
# 4. 测试与调试文件（可选排除）
# -----------------------------------------------------------------------------
coverage/
.nyc_output/
test-results/
playwright-report/

# -----------------------------------------------------------------------------
# 5. 所有不上传公开的其它无分类的文件，比如AI提示词记忆总结文件
# -----------------------------------------------------------------------------
DO_NOT_PUBLIC/

# -----------------------------------------------------------------------------
# 6. 允许上传的文件清单（GitHub Pages 必需）
# -----------------------------------------------------------------------------
# ✅ HTML 文件：index.html, about.html, products.html 等
# ✅ CSS 样式：css/*.css
# ✅ JavaScript: js/*.js
# ✅ 图片资源：images/**/*
# ✅ 文档文件：README.md, LICENSE
# ✅ SEO 配置：robots.txt, sitemap.xml, llms.txt
# ✅ 字体文件：*.woff, *.woff2, *.ttf, *.eot
# ✅ package.json（用于记录项目信息）
#
# ❌ 不上传：敏感配置、依赖包、源文件、临时文件
# =========================================================================



# =========================================================================
# 使用说明
# =========================================================================
# 1. 将此文件保存为 .gitignore（注意前面的点）
# 2. 放置在项目根目录
# 3. Git 会自动忽略列出的文件和目录
# 4. 如需修改，编辑此文件即可
# 5. 已跟踪的文件不会被忽略，需要使用 git rm --cached <file> 移除跟踪

# 验证命令：
# git check-ignore -v <filename>  # 检查某个文件为何被忽略
# git status                      # 查看当前状态，确认忽略生效
# git status --ignored            # 查看当前状态，确认忽略生效

# =========================================================================
# 最佳实践建议
# =========================================================================
# 1. 只提交源代码和必要配置（HTML/CSS/JS/图片/文档）
# 2. 不提交编译产物和依赖包（node_modules）
# 3. 不提交敏感信息和密钥（.env, *.key, *.pem）
# 4. 不提交本地开发工具配置（.lingma/, .idea/, .vscode/）
# 5. 定期清理无用的临时文件（*.tmp, *.log）
# 6. 团队协作前统一 .gitignore 配置
# =========================================================================
