---
interface Props {
  zones: Array<any>;
}
const { zones } = Astro.props as Props;
---

<section class="py-16 bg-white">
  <div class="container mx-auto px-4">
    <div class="text-center mb-12">
      <h2 class="text-3xl font-bold text-primary-dark mb-4">探索更多系列产品</h2>
      <p class="text-neutral max-w-2xl mx-auto">浏览其他润滑油系列产品, 找到适合您设备需求的润滑解决方案</p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
      {zones.map((relatedZone) => (
        <div class="group">
          <a
            href={`/zones/${relatedZone.id}`}
            class="block cursor-pointer"
            title={`了解 ${relatedZone.data.title}`}
            aria-label={`了解 ${relatedZone.data.title}`}
          >
            <div class="relative overflow-hidden rounded-lg elegant-shadow hover:elegant-shadow-lg transition-all duration-300">
              <img
                src={relatedZone.data.thumbnail}
                alt={relatedZone.data.title}
                loading="lazy"
                class="w-full h-64 object-cover group-hover:scale-105 transition-transform duration-300"
              />
              <div class="absolute inset-0 bg-gradient-to-t from-black/60 to-transparent"></div>
              <div class="absolute bottom-0 left-0 right-0 p-6 text-white">
                <h3 class="text-xl font-semibold mb-2">{relatedZone.data.title}</h3>
                <p class="text-white/90 text-sm line-clamp-2">{relatedZone.data.description}</p>
              </div>
            </div>
          </a>
          <a
            href={`/zones/${relatedZone.id}`}
            class="block mt-4 text-center text-primary hover:text-primary-soft transition-colors font-medium"
            aria-label={`了解 ${relatedZone.data.title}`}
          >
            了解 {relatedZone.data.title} →
          </a>
        </div>
      ))}
    </div>
  </div>
</section>
